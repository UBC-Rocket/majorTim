version: 2
jobs:
  build:
    docker:
      - image: ubcrocket/majortim-build
    steps:
      - checkout
      - run:
          name: Build project
          command: |
            ./dmake.sh apdet-hanna
            ./dmake.sh sharetest

      - store_artifacts:
          path: /builds/
